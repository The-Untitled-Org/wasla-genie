# MoM 20260515
- let's add openclaw in the grilling session, instead of opencode as an example of the extreme usecase.
- content mirroring + strategy of sync (latest is greatest).
- transformers concept for maintaining (new updates for any vendor).
- (content mirror + daemon) - Tool-open trigger only
- daemon: executable distributed: `npx <script.js>`
- leave it for the final user to sync
- user <not focus>
    cwd/.wasla/default
        cwd/.gemini
        cwd/.claude
    ~/.wasla/default
        ~/.gemini
        ~/.claude
        ~/.opencode


  The features now in play:
  1. Local agent creation in familiar tools (zero-friction)
  2. Local sync via daemon (agents visible everywhere)
  3. Gradual centralization to ~/.waslgenie/ (portability)
  4. Export/import for backup (individual machine)
  5. Team shared repo (team collaboration — NEW)
  6. Conflict resolution (local + team conflicts?)
  7. Multiple stub strategies per tool (A/B/C)
  8. Settings/MCPs (post-MVP but affects registry design)